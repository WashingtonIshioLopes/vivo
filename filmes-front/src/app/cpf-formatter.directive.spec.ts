import { CpfFormatterDirective } from './cpf-formatter.directive';

describe('CpfFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CpfFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
